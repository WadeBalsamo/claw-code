Your job is to fix the ./setup_ollamacode_client.sh script ; it needs to setup a shortcut for ollamacode-client , which should list the models available on ollama, and if a model is selected but not running on the server the client should be able to send a signal to start models on the server, and then start open-claw using these ollama models on the local ollama server. You can see the ./setup_opencode_shortcut.sh is working correctly to launch claw-code from an openrouter api. we need to do this with ollama. You may need to fix ./setup_ollamacode_server.sh as well as ./setup_ollamacode_client.sh ; think this through step by step and reason around the process. The ollamacode-client should be able to access ollama running on local, as well as ollama running on ollamacode-server on another device on this network. Make sure you test the script by running setup_ollamacode_client.sh && ollamacode-client ; make sure the script shows what ollama models are available on local ollama on this device, and also looks for ollama served on the network from an ollamacode-server (you can just detail the api specs for that in ollamacode-server.txt). Your end condition is when you run ./setup_ollamacode_client.sh && ollamacode-client ; select the local ollama on this device, start the model nemotron-3-nano:4b, the script should launch claw-code properly, and then you can prompt "write me a poem.txt". If a poem.txt appears as generated from the ollama model without any api errors, then, you are successful, until then, continue fixing setup_ollamacode_client.sh